{
  "name": "signarchive",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-models.js",
    "sync:watch": "node scripts/sync-models.js --watch",
    "dev:backend": "npm run sync && cd backend && pnpm dev",
    "dev:frontend": "npm run sync && cd frontend && pnpm dev",
    "dev": "npm run sync && concurrently \"cd backend && pnpm dev\" \"cd frontend && pnpm dev\"",
    "postinstall": "cd backend/node_modules/better-sqlite3 && npm run build-release"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "react-router-dom": "^7.14.0"
  }
}
